import { EmptyState } from "@/components/ui/empty-state";

export default function MyDealsPage() {
  return (
    <EmptyState
      title="My Deals"
      description="Saved properties, folders, progress stages, notes, tasks, scenarios, and shared links, once you're signed in."
      note="My Deals is built in Milestone 6."
    />
  );
}
