import { EmptyState } from "@/components/ui/empty-state";

export default function ProfilePage() {
  return (
    <EmptyState
      title="Profile"
      description="Optional preferences, financial ranges, saved destinations, notifications, appearance, data export, and account deletion."
      note="Profile preferences and data controls are built in Milestone 6."
    />
  );
}
