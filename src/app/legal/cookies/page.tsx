import { EmptyState } from "@/components/ui/empty-state";

export default function CookiePolicyPage() {
  return (
    <EmptyState
      title="Cookie Policy"
      description="Which cookies and browser storage DealFactor uses, and why — authentication, security, appearance, and guest sessions only."
      note="Draft legal content is written in Milestone 10 and requires qualified legal review before real public launch."
    />
  );
}
