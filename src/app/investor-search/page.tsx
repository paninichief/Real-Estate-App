import { EmptyState } from "@/components/ui/empty-state";

export default function InvestorSearchPage() {
  return (
    <EmptyState
      title="Investor Search"
      description="Natural-language and strategy-based search for residential investment properties, with grid, list, map, and split views."
      note="Search and results land in Milestone 4."
    />
  );
}
